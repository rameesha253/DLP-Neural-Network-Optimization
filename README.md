# DLP-Neural-Network-Optimization
An end-to-end deep learning project analyzing Multi-Layer Perceptrons on Fashion-MNIST. Features manual NumPy backpropagation verified against PyTorch, activation/loss dynamics, first-order optimization benchmarks, bias-variance control via regularization, and hyperparameter tuning with 5-fold cross-validation.
